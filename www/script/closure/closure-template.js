(function($, undefined) {
// Private class vars.
// all declared vars here are private to this "class".

// Declaration of the class inside a namespace $.ia in our case.
// Now we can extend from the jQuery.ia namespace.
	
$.ia.imywaClass= {
	// object attributes and methods. 
	// Private are prefixed with _
};
	
/* to extend by function 

	function extends(namespace, object){
		$.ia[namespace]= object;
	}

*/

}) (jQuery);